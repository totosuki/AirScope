# AirScope AGENTS.md

このファイルは、ADS-B 航空機可視化システム「AirScope」を開発する AI エージェント向けの実装計画書である。以後の作業では、本書を最初に読み、ここに記載した方針に従って進めること。

## 1. プロジェクト概要

AirScope は、Raspberry Pi 4 と RTL-SDR で受信した ADS-B 1090 MHz 信号を dump1090-fa でデコードし、Azure 上に送信・蓄積して、Azure Static Web Apps 上の Web ダッシュボードでリアルタイム可視化するシステムである。並行して fr24feed を動作させ、Flightradar24 へ受信データを提供する。

主な目的は以下の通り。

- 自宅周辺で受信した航空機の位置・高度・速度・コールサイン等を地図上に可視化する。
- 講義・実験セッション期間中の航空機受信ログと統計を蓄積・表示する。
- fr24feed により、受信データを Flightradar24 のグローバル追跡網へ貢献する。

## 2. 開発運用方針

- 個人開発のため、当面は `main` ブランチのみで開発する。
- Issue と Pull Request は使用しない。
- 作業前に `git status --short` を確認し、既存の未追跡・変更ファイルを把握する。
- ユーザーが作成した変更は勝手に戻さない。
- 破壊的な操作、機密情報の削除、環境設定の大幅変更が必要な場合は、必ず事前に確認する。
- 依存関係の追加は、既存構成と実装目的に対して必要性が明確な場合のみ行う。

## 3. 書類作成ルール

`documents` ディレクトリはローカル書類置き場として扱い、Git には載せない。ユーザーが「書類を作成して」「作業記録を書いて」など、明示的に書類作成を依頼した場合のみ Markdown 書類を作成する。通常の実装・修正作業では、書類を自動作成しない。

### 3.1 日付確認

書類を作成・更新する場合は、必ず現在日付を確認する。ローカル環境で作業する場合は、原則として次のコマンドで確認する。

```sh
date +%F
date +%y%m%d
```

### 3.2 ファイル名

書類は日付ごとのディレクトリに格納し、ファイル名はその日の連番と内容が分かる短い日本語名で構成する。

```text
documents/yyMMdd/<その日付の書類番号>-<内容が分かる日本語名>.md
```

例: `documents/260608/001-AirScope初期計画.md`

`yyMMdd` は作成・更新時に確認した現在日付を使う。書類番号は同日の `documents/yyMMdd/*.md` を確認し、3桁の連番で採番する。書類番号の後ろには、`AirScope初期計画`、`エッジデモ送信実装報告` のように、書類の内容が日本語で分かる短い名称を付ける。Azure、IoT Hub、Cosmos DB などの製品名・技術名は、識別に必要な範囲で使用してよい。

Markdown 書類の先頭に記載する H1 タイトルも、日本語で内容が分かる名称にする。

既存書類へ追記する作業か、新規書類に分ける作業かは、ユーザーの依頼内容と書類の目的で判断する。

### 3.3 記録内容

各書類には、少なくとも以下を記録する。

- 作成日・更新日
- 作業者
- 作業目的
- 参照した資料
- 実施内容
- 変更したファイル
- 検証結果
- 次の作業候補

### 3.4 Git 管理する正式書類

`documents` は日々の作業記録や調査資料を置くローカル専用ディレクトリとし、Git には載せない。実装と一緒に継続的に保守する設計書、仕様書、運用手順書は `docs` に配置して Git 管理する。

正式書類は、目的に応じて `docs/設計`、`docs/運用` などに分類する。ファイル名と H1 タイトルは、`データベース設計.md` のように日本語で内容が分かる名称にする。日付別ディレクトリと連番は不要である。

コード、Azure 構成、データスキーマなどが正式書類の記載とずれる変更を行う場合は、同じ作業で該当書類も更新する。

## 4. 当面のモノレポ構成

当面は次の3つの実装ディレクトリを中心に進める。Azure Functions、IaC、共通パッケージなどは、必要性が明確になった時点で追加する。初期段階では、将来使うかもしれない空の管理単位を増やさない。

```text
AirScope/
├── AGENTS.md
├── README.md
├── docs/
│   └── 設計/
│       └── データベース設計.md
├── documents/
│   ├── 企画書.pdf
│   └── 260608/
│       └── 001-AirScope初期計画.md
├── edge/
├── functions/
├── fr24/
└── web/
```

### 4.1 各ディレクトリの責務

- `edge`: Raspberry Pi でのデータ受信から Azure IoT Hub 送信までを管理する。dump1090-fa の `aircraft.json` 読み取り、デモデータ生成、整形・重複除去、Azure IoT Hub 送信、任意 HTTP エンドポイントへのデモ送信をここに含める。
- `functions`: Azure IoT Hub からのテレメトリ取り込み、Cosmos DB への履歴・最新状態保存、将来の Web API を管理する。
- `fr24`: Raspberry Pi で受信したデータを Flightradar24 へ contribute するための設定、手順、補助スクリプトを管理する。fr24feed 公式クライアント本体や認証情報は含めない。実装は後半フェーズでよい。
- `web`: Azure Static Web Apps にデプロイする Web アプリを管理する。Next.js、Leaflet、Chart.js による地図、受信ログ、統計ダッシュボードをここに含める。
- `docs`: Git 管理する正式な設計書、仕様書、運用手順書を管理する。

### 4.2 当面は作らないもの

次のディレクトリは将来的に必要になり得るが、初期段階では作らない。

- `infrastructure`: Azure IaC や Raspberry Pi セットアップをコード化する段階で追加する。
- `packages`: Web と edge の間で共有する型やスキーマが増えた段階で追加する。
- `scripts`: リポジトリ横断の補助スクリプトが必要になった段階で追加する。
- `.github`: CI/CD を使う段階で追加する。個人開発かつ PR 不使用のため、現時点では不要。

## 5. データ設計の基本方針

ADS-B テレメトリは、まず次のフィールドを中心に扱う。

- `icao24`: ICAO 24bit アドレス
- `callsign`: コールサイン
- `lat`, `lon`: 緯度・経度
- `altitude_ft`: 高度
- `ground_speed_kt`: 対地速度
- `track_deg`: 進行方向
- `vertical_rate_fpm`: 垂直速度
- `squawk`: スコーク
- `seen_at`: dump1090 側での観測時刻
- `received_at`: AirScope が受信・処理した時刻
- `receiver_id`: 受信局 ID
- `distance_km`: 受信地点からの推定距離

Cosmos DB では、セッション内の時系列表示と機体単位のログ集計を両立できるよう、パーティションキーを慎重に設計する。第一候補は `session_id` と `icao24` の組み合わせを検討する。

## 6. 実装フェーズ

### Phase 0: リポジトリ基盤

- `README.md` を作成し、AirScope の目的、全体構成、ローカル開発手順を記述する。
- 当面のモノレポ構成を `edge`、`fr24`、`web` の3本に整理する。
- 共通の命名規則、環境変数、秘密情報の扱いを整理する。
- サンプル ADS-B JSON とスキーマを用意する。

### Phase 1: Raspberry Pi エッジ収集

- dump1090-fa の `aircraft.json` を読み取る Python コレクタを実装する。
- 緯度・経度がない機体、古い観測値、重複メッセージを除外・整形する。
- Azure IoT Hub へ MQTT または SDK 経由で送信する。
- ローカルファイル出力モードを用意し、Azure 未接続でも動作検証できるようにする。

### Phase 2: Azure 取り込み・蓄積

- IoT Hub から Cosmos DB へ保存する取り込み処理を実装する。
- Cosmos DB のコンテナ、インデックス、TTL、パーティションキーを定義する。
- セッション期間中のデータ保持を前提に、必要なら TTL を設定する。
- 接続文字列などの秘密情報は `.env` や Azure 側の設定に置き、リポジトリへコミットしない。

### Phase 3: Web ダッシュボード

- Next.js アプリを `web` に作成する。
- Leaflet でリアルタイム航空機地図を実装する。
- 高度帯に応じて機影アイコンの色を変える。
- 機体クリック時にコールサイン、ICAO、速度、高度、最終観測時刻などを表示する。
- 受信ログ一覧と統計グラフを実装する。
- Static Web Apps で配信できる構成にする。

### Phase 4: FR24 連携

- fr24feed の導入手順、設定テンプレート、systemd 起動例を整理する。
- AirScope 本体の Azure パイプラインとは独立して動く前提を守る。
- fr24feed の認証情報、共有キー、受信局固有情報はリポジトリに含めない。

### Phase 5: 統合検証

- サンプル JSON によるエッジコレクタ単体テストを実施する。
- Azure への送信をモックまたは開発環境で検証する。
- Cosmos DB 保存データを Web で表示する E2E 経路を確認する。
- Raspberry Pi 上で dump1090-fa、`edge` 内の送信処理、fr24feed の同時動作を確認する。

## 7. 技術選定

- エッジ処理: Python 3.11 以降を第一候補とする。
- Web: Next.js、TypeScript、Leaflet、Chart.js を第一候補とする。
- Azure 取り込み: Azure Functions を第一候補とする。
- IaC: Bicep を第一候補とする。
- パッケージ管理は、各サブプロジェクトの標準に従う。Node.js では `package-lock.json`、Python では `pyproject.toml` を基本とする。

## 8. Devcontainer 方針

軽量な devcontainer を開発・検証環境の標準化に使う。初期構成では Python 3.12 と Node.js 22 を提供し、`edge` の Python 開発と `web` の Next.js 開発を支援する。

Azure CLI、Azure Functions Core Tools、Static Web Apps CLI は初期 devcontainer には含めない。必要性が明確になった段階で追加する。

devcontainer で扱うもの:

- `edge` のデモ送信処理
- `web` のローカル開発サーバー
- サンプル JSON やスキーマ検証
- Python / Node.js のバージョンを揃えたローカル検証

devcontainer で扱わないもの:

- RTL-SDR 実機の USB 受信
- dump1090-fa の実受信確認
- fr24feed の本番登録・稼働
- Raspberry Pi 固有の systemd 動作確認

Codex で作業する場合、devcontainer 内で実行しているかどうかで利用できるコマンドや依存関係が変わる。必要に応じて `python --version`、`node --version`、`test -f /.dockerenv` などで実行環境を確認する。

## 9. 機密情報と安全性

- IoT Hub 接続文字列、Cosmos DB キー、FR24 共有キー、受信局 ID などはコミットしない。
- `.env.example` にはダミー値のみを書く。
- 実 ADS-B データを保存する場合は、公開情報であっても必要以上に長期保持しない。
- 受信専用システムとして扱い、送信機能や無線機制御機能を追加しない。

## 10. 法的・倫理的配慮

ADS-B は公開ブロードキャストであり、AirScope は受信専用として設計する。ただし、画面やドキュメントでは、取得データが航空機運航に関する公開情報であること、個人追跡を目的にしないこと、運航安全へ影響する用途には使わないことを明確にする。

## 11. 作業時の検証方針

- Python 変更時は、該当サービスの単体テストと型・lint を可能な範囲で実行する。
- Web 変更時は、ビルド、lint、主要画面の表示確認を実行する。
- IaC 変更時は、構文検証または dry-run 相当のコマンドを実行する。
- 検証できなかった項目は、最終回答で理由と残リスクを説明する。書類作成を明示的に依頼されている場合のみ、作業記録書類にも残す。
